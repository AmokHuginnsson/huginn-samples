#! /bin/sh
exec huginn --no-argv -E "${0}" "${@}"
#! huginn

main() {
	while ( size( line = input().strip() ) > 0 ) {
		x = integer( line );
		if ( x > 10 ) {
			print( line + " is too much for me :(\n" );
			break;
		}
		print( line + " squared is " + string( x * x ) + "\n" );
	}
	return ( 0 );
}
